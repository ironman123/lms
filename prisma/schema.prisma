// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider   = "prisma-client-js"
  engineType = "library"
  //output   = "../lib/generated/prisma"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

// This is the structure you should use for the Exams/Syllabus
model Exam {
  id             String          @id @default(uuid())
  name           String          @unique // e.g., "GATE CSE"
  description    String?
  duration       Int
  totalMarks     Int
  color          String          @default("#0076c5") // Hex color code for UI representation
  categories     Category[] // An exam has many categories (Engineering Math, OS, etc.)
  questionPapers QuestionPaper[]
}

model Category {
  id     String  @id @default(uuid())
  name   String // e.g., "General Aptitude"
  exam   Exam    @relation(fields: [examId], references: [id])
  examId String
  topics Topic[] // A category has many topics (Verbal Ability, Numerical)
}

model Topic {
  id         String     @id @default(uuid())
  name       String // e.g., "Linear Algebra"
  category   Category   @relation(fields: [categoryId], references: [id])
  categoryId String
  questions  Question[]
}

enum QuestionType {
  MCQ // Single correct answer
  MSQ // Multiple Select (Multiple correct answers)
  NUMERICAL // Integer or Decimal input (Common in GATE)
  SUBJECTIVE // Text-based long answers (Common in UPSC/KPSC)
}

model QuestionPaper {
  id        String     @id @default(uuid())
  title     String // e.g., "GATE CSE 2024 Mock 1"
  year      Int?
  exam      Exam       @relation(fields: [examId], references: [id])
  examId    String
  questions Question[]
  createdAt DateTime   @default(now())
}

model Question {
  id            String       @id @default(uuid())
  content       String // The actual question text (Markdown/KaTeX supported)
  type          QuestionType
  marks         Float        @default(1.0)
  negativeMarks Float        @default(0.0)
  explanation   String? // Solution/Hint for the student

  // Relations
  topic    Topic          @relation(fields: [topicId], references: [id])
  topicId  String
  paper    QuestionPaper? @relation(fields: [paperId], references: [id])
  paperId  String?
  subTopic String? // Optional sub-topic for better organization (e.g., "Eigenvalues" under "Linear Algebra")
  // Options for MCQ/MSQ
  options  Option[]

  // For Numerical/Subjective correct answers
  correctAnswer String? // Stores the number or the model answer string
}

model Option {
  id         String   @id @default(uuid())
  text       String
  isCorrect  Boolean  @default(false)
  question   Question @relation(fields: [questionId], references: [id], onDelete: Cascade)
  questionId String
}
